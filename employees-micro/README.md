# Thorntail példa alkalmazás

Az `mvn thorntail:run` paranccsal futtatható.

Szükséges adatbázis: `jdbc:mariadb://localhost:3306/employees`, `employees`, `employees`.
A `project-defaults.yml` fájlban konfigurálható.